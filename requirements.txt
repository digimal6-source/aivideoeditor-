# Core runtime dependencies.
#
# The HTTP API, routing, multipart upload parsing, job management and all
# configuration/validation code run on the Python standard library alone -
# there is deliberately no web framework to install, so the app starts in a
# fresh Codespace in seconds and has almost no supply chain.
#
# These two are genuinely needed by the render pipeline:
Pillow>=10.0.0        # hook text layer rendering (transparent PNG overlay)
fonttools>=4.40.0     # reads a font's internal family name; validates uploads

# Speech-to-text (faster-whisper) is intentionally NOT here. It pulls in
# ctranslate2 + tokenizers + onnxruntime (~200 MB) and the app is fully usable
# without it via the "Paste transcript manually" caption source.
#
#   pip install -r requirements-optional.txt
